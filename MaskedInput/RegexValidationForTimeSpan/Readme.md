##Regex Validation for TimeSpan##
RegexValidationForTimeSpan SDK sample shows how to use the custom regex applied to the ValidationRegex,to validate TimeSpan input.

<keywords: validationregex, maskedtextinput >
